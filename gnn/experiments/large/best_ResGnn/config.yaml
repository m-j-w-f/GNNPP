data:
  method: "max_dist"
  max_dist: 200
  nearest_k: 0
  nearest_k_mode: "in"
  use_corr: False

model:
  architecture: "ResGnn"
  num_layers: 1
  batch_size: 8
  embed_dim: 10
  hidden_channels: 256
  heads: 1
  compile: True
  lr: 0.001

training:
  n_epochs: 89
  patience: 89