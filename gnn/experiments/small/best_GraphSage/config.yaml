data:
  method: "max_dist"
  max_dist: 200
  nearest_k: 0
  nearest_k_mode: "in"
  use_corr: False

model:
  architecture: "GraphSAGE"
  num_layers: 2
  batch_size: 8
  embed_dim: 10
  hidden_channels: 512
  heads: 0
  compile: True
  lr: 0.001

training:
  n_epochs: 187
  patience: 187